#if !defined(_TIMEMARKEDDATA_H)
#define _TIMEMARKEDDATA_H

typedef struct TimeMarkedData TimeMarkedData;
struct TimeMarkedData {
    int timeInterval;
    int dataValue;
};


#endif // _TIMEMARKEDDATA_H
